apiVersion: v1
kind: Service
metadata:
  name: microservice-one
  namespace: sample-ns
  annotations:
   alb.ingress.kubernetes.io/healthcheck-path: /microservice-one/
spec:
  selector:
    app: microservice-one
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 32000
  type: NodePort
